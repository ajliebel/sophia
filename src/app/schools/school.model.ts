export class School {
    public name: string;
    public period: string;
    public imageUrl: string;

    constructor(name: string, period: string, imageUrl: string) {
        this.name = name;
        this.period = period;
        this.imageUrl = imageUrl;
    }
}